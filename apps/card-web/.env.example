# Root preview uses the generic placeholder tenant by default.
# Explicit /c/:slug or ?tenant= selects a registered enterprise instead.
VITE_DEFAULT_TENANT=template

# Optional local review route: /c/:slug?mock-card=employee|enterprise.
# Keep disabled in production builds unless a temporary visual review requires it.
VITE_ENABLE_CARD_MOCK=false
